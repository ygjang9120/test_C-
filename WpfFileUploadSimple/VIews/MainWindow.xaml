<Window x:Class="WpfFileUploadSimple.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfFileUploadSimple.ViewModels"
        mc:Ignorable="d"
        Title="파일 업로드 (MVVM + Service)" Height="400" Width="600">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="사용자 이름:" VerticalAlignment="Center" FontWeight="Bold"/>
                    <TextBox Grid.Column="1" Margin="10,0" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsLoggedIn, Converter={StaticResource InverseBooleanConverter}}"/>
                    <Button Grid.Column="2" Content="로그인" Width="80" Command="{Binding LoginCommand}"/>
                </Grid>
                <Grid Margin="0,10,0,0">
                    <TextBlock Text="{Binding UserName, StringFormat='현재 사용자: {0}'}" Foreground="SteelBlue" FontWeight="Bold" VerticalAlignment="Center" Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Button Content="로그아웃" Width="80" HorizontalAlignment="Right" Command="{Binding LogoutCommand}" Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}" Background="OrangeRed" Foreground="White"/>
                </Grid>
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="1" Header="파일 업로드" Margin="0,10,0,0" IsEnabled="{Binding IsLoggedIn}">
            <StackPanel Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="파일 선택..." Width="100" Command="{Binding SelectFileCommand}"/>
                    <TextBlock Grid.Column="1" Text="{Binding SelectedFileName}" VerticalAlignment="Center" Margin="10,0" Foreground="Gray"/>
                </Grid>
                <Button Content="업로드 실행" Width="100" Height="30" Margin="0,10,0,0" HorizontalAlignment="Left" Command="{Binding UploadCommand}" FontWeight="Bold"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="업로드 로그" Margin="0,10,0,0">
            <ListBox ItemsSource="{Binding LogMessages}"/>
        </GroupBox>
    </Grid>
</Window>